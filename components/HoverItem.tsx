import React, { useState } from 'react';
import { View, Text } from 'react-native'

const HoverItem = () => {
    return (
        <View>
            <Text>Hover element</Text>
        </View>
    )
};

export default HoverItem;